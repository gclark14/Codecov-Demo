language: java

jdk:
  - oraclejdk8

before_script:
  - chmod +x gradlew

script:
  - ./gradlew check
  - ./gradlew clean test testIntegration jacocoTestReport

after_success:
  - bash <(curl -s https://codecov.io/bash) -F unittests -f build/reports/jacoco/test/jacocoTestReport.xml
  #./gradlew testIntegration jacocoTestReport
  - bash <(curl -s https://codecov.io/bash) -F integrationTests -f build/reports/jacoco/test/jacocoTestReport.xml

